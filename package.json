{
  "name": "pushsocket.js",
  "version": "1.0.1",
  "description": "An Open-Sourced remix of SimpleSocket, built from Scratch.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc dist/* && mv ./client-npm.js ./src/ && mv ./SGen.js ./src/",
    "build:docs": "npx typedoc ./dist/* && mv ./docs ./repo2",
    "deploy:docs": "git config --global user.email \"test@test.com\" && git config --global user.name \"test\" && git add . && git commit -m \"docs: Deploy docs\" && git push -u origin master"
  },
  "keywords": [],
  "author": "XAPK Enterprises",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^3.2.6",
    "typedoc": "^0.25.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.4"
  }
}
